# docker compose for retrieving jenkins build info into elasticsearch

This repo can be used to create a EBK (eleasticsearch, beats, kibana)
docker compose instance for extracting job data into elasticsearch

